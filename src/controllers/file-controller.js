const FileService = require('../services/file-service')

const FileController = {
    getFile(req, res) {
        console.log(req)
    },
    createFile(req, res) {
        console.log(req)
    }
}

module.exports = FileController